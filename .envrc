# Loaded by direnv when you enter this directory.
# Pulls NEXT_PUBLIC_GAME_URL (and any other vars) from the git-ignored
# .env.local into your shell, so scripts and builds see the same config.
dotenv_if_exists .env.local